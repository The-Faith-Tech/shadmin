import 'package:flutter/material.dart';

class SHSiderbarTile extends StatelessWidget {
  const SHSiderbarTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}